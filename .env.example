# FTM Studio is a fully local Chrome extension and currently requires no
# runtime environment variables or service credentials.
# Keep any future secrets in a server-side environment only; never expose
# service-role, secret, database, OAuth, or JWT-signing values to the client.
